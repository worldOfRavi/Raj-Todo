import { createTheme } from "@mui/material";


const theme  = createTheme({
    palette : {
        primary: {
            main: "#7d8581",
          },
          secondary: {
            main: "#cbf5e1",
          },
    }
})

export default theme;